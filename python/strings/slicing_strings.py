sample_url="http://google.com"
print(sample_url)

# reverse the url
print(sample_url[::-1])

#get the top level domain
print("top level domain is")
print(sample_url[-4:])

#print the url without the http
print(sample_url[7:])